Update `tests/sign-in.spec.ts` to verify that the dashboard welcome message is visible after sign-in.

Requirements:
- Use role-based locators.
- Keep the existing sign-in flow.
- Do not use page.waitForTimeout().
- Limit changes to `tests/sign-in.spec.ts`.

Do not:
- Modify Playwright configuration.
- Add retries.
- Create helper methods.
- Refactor unrelated code.
